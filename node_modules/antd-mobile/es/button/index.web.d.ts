/// <reference types="react" />
import React from 'react';
import { ButtonWebProps } from './PropsType';
declare class Button extends React.Component<ButtonWebProps, any> {
    static defaultProps: {
        prefixCls: string;
        size: string;
        inline: boolean;
        across: boolean;
        disabled: boolean;
        loading: boolean;
        activeStyle: {};
    };
    render(): JSX.Element;
}
export default Button;
